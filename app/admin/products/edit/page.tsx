import React from 'react';

export default async function Page({ params }:{ params: { id: string } }) {;

  return (
    <div>
      <h1>Edit Product:</h1>

    
      {/* Add your form for editing the product here */}
    </div>
  );
}


